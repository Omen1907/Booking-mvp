import BookingForm from "../BookingForm/BookingForm";

const Book = () => {
  return (
    <div className="min-h-screen flex justify-center items-center bg-gradient-to-br from-purple-100 via-pink-100 to-yellow-100 p-6">
      <BookingForm />
    </div>
  );
};

export default Book;
